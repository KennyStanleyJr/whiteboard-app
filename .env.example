# Supabase (optional - required for Save/Load to cloud)
# Get these from your project at https://supabase.com/dashboard
# VITE_ prefix is required so Vite exposes these to the browser. Only the anon (public)
# key belongs here. Never put the service_role key in a VITE_ variable.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
