# tldraw license key (optional – for commercial use; see https://tldraw.dev/pricing)
# VITE_ prefix is required so Vite exposes this to the browser.
# VITE_TLDRAW_LICENSE_KEY=your-license-key

# Supabase (optional – required for Share page)
# Get these from your project at https://supabase.com/dashboard
# VITE_SUPABASE_URL=https://xxx.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key

# Sync server (optional – for real-time collaboration; deploy sync-server to Render etc.)
# WebSocket URL without path, e.g. wss://your-app.onrender.com or https://your-app.onrender.com
# VITE_SYNC_SERVER_URL=wss://your-sync-server.onrender.com
# The sync server loads this .env for local dev (uses VITE_SUPABASE_*). On Render, set SUPABASE_URL and SUPABASE_ANON_KEY.
